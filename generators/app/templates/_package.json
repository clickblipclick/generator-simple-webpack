{
  "private": true,
  "engines": {
    "node": ">=4"
  },
  "scripts": {
    "start": ""
  },
  "dependencies": {<% if (includeReact) { %>
    "react": "^16.2.0",
    "react-dom": "^16.2.0"<% } %>
  },
  "devDependencies": {
    "autoprefixer": "^8.1.0",
    "webpack": "^4.2.0",<% if (includeBabel) { %>
    "babel-core": "^6.26.0",
    "babel-eslint": "^8.2.2",
    "babel-loader": "^7.1.4",
    "babel-preset-env": "^1.6.1",<% } %>
    "css-loader": "^0.28.11",
    "eslint": "^4.19.0",
    "eslint-loader": "^2.0.0",
    "html-webpack-plugin": "^3.0.7",<% if (includeSass) { %>
    "node-sass": "^4.7.2",
    "sass-loader": "^6.0.7",<% } %>
    "style-loader": "^0.20.3",
    "webpack": "^4.2.0",
    "webpack-dev-server": "^3.1.1"
  },<% if (includeBabel) { %>
  "babel": {
    "presets": ["env"<% if (includeReact) { %>,"react"<% } %>]
  },<% } %>
  "browserslist": [
    "> 1%",
    "last 2 versions"
  ],
  "eslintConfig": {
    "env": {<% if (includeBabel) { %>
      "es6": true,<% } %>
      "node": true,
      "browser": true
    }
  }
}